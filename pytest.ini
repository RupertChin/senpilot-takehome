[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = -q
markers =
    live: tests that hit the live UARB site (slow; require network egress)
    llm: tests that call the live Anthropic API (require ANTHROPIC_API_KEY)
